{
  "name": "devbox-poc",
  "version": "0.3.0",
  "description": "AI编码agent自动实现和回测交易策略",
  "workspaces": [
    "packages/*",
    "apps/*"
  ],
  "bin": {
    "devbox": "bin/devbox"
  },
  "scripts": {
    "dev": "npm run link:claude && echo '✓ Skill已安装到 ~/.claude/skills/devbox'",
    "link:claude": "mkdir -p ~/.claude/skills && ln -sfn $(pwd)/skills/devbox ~/.claude/skills/devbox",
    "link-skill": "node scripts/link-skill.js",
    "test": "jest",
    "test:watch": "jest --watch",
    "test:coverage": "jest --coverage"
  },
  "keywords": [
    "devbox",
    "agent",
    "backtest",
    "trading",
    "claude-code",
    "skill"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/devbox-poc.git"
  },
  "dependencies": {
    "commander": "^14.0.3",
    "execa": "^9.6.1"
  },
  "devDependencies": {
    "@types/jest": "^29.5.12",
    "jest": "^29.7.0",
    "ts-jest": "^29.1.2"
  }
}
