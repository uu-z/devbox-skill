{
  "name": "devbox-poc",
  "version": "0.3.0",
  "description": "AI编码agent自动实现和回测交易策略",
  "bin": {
    "devbox": "bin/devbox"
  },
  "scripts": {
    "postinstall": "mkdir -p ~/.claude/skills && ln -sfn $(npm root -g)/devbox-poc/skills/devbox ~/.claude/skills/devbox && echo '✓ devbox 命令和 skill 已安装'",
    "dev": "npm run link:claude && echo '✓ Skill已安装到 ~/.claude/skills/devbox'",
    "link:claude": "mkdir -p ~/.claude/skills && ln -sfn $(pwd)/skills/devbox ~/.claude/skills/devbox"
  },
  "keywords": [
    "devbox",
    "agent",
    "backtest",
    "trading",
    "claude-code",
    "skill"
  ],
  "author": "",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/yourusername/devbox-poc.git"
  }
}
