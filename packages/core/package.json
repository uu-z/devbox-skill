{
  "name": "@devbox/core",
  "version": "1.0.0",
  "type": "module",
  "description": "Devbox core library - interfaces and implementations",
  "main": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./interfaces": "./src/interfaces/index.ts",
    "./docker": "./src/docker/index.ts",
    "./protocol": "./src/protocol/index.ts"
  },
  "scripts": {
    "build": "echo 'Core is TypeScript only, no build needed'",
    "test": "bun test",
    "lint": "echo 'Lint not configured yet'",
    "clean": "rm -rf node_modules"
  },
  "keywords": ["devbox", "vm", "docker", "provisioner"],
  "license": "MIT"
}
